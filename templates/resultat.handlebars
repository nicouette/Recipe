<html>
{{> head}}

<body>
  <header>
    {{> nav}}
    <h1>Mon carnet de Recettes</h1>
  </header>
  <p>
    <!-- Voici les résultats qui correspondent à votre recherche:-->
    <table>
      <tr>
        <th width="500px">Nom</th>
        <th>Type</th>
        <th>Photo</th>
      </tr>
      {{#each lines}}
      <tr>
        <td width="500px">
          <div class=linkRes><a href="recette?id={{id}}">{{nom}}</div></a></td>
        <td>{{type}}</td>
        <td></td>
      </tr>
      {{/each}}

    </table>
    {{> footer}}
</body>

</html>
